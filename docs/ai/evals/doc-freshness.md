# Documentation Freshness Eval

Ask an agent to answer:

1. Where is release default config stored?
2. What is the difference between refresh and scan?
3. Which TOML table stores Free Links?
4. Which command verifies the auto-test profile?
5. Which file owns tree grouping logic?

The expected answers should be traceable to current docs and code.
